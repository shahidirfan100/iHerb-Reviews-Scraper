{
    "actorOutputSchemaVersion": 1,
    "title": "iHerb Reviews Output",
    "properties": {
        "reviews": {
            "type": "string",
            "title": "Product Reviews",
            "description": "Complete product reviews with all details",
            "template": "{{links.apiDefaultDatasetUrl}}/items?view=overview"
        },
        "reviewsCSV": {
            "type": "string",
            "title": "Download as CSV",
            "description": "Export reviews in CSV format",
            "template": "{{links.apiDefaultDatasetUrl}}/items?format=csv"
        },
        "reviewsJSON": {
            "type": "string",
            "title": "Download as JSON",
            "description": "Export reviews in JSON format",
            "template": "{{links.apiDefaultDatasetUrl}}/items?format=json"
        },
        "statistics": {
            "type": "string",
            "title": "Statistics",
            "description": "Scraping statistics and summary",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/keys/statistics"
        }
    }
}
